{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "test": "lerna run test",
    "start": "lerna run start --scope=server-app"
  }
}
